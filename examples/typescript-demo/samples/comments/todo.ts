// TODO: replace this stub before merging.
export function placeholder(): number {
  return 0;
}

// TODO(PROJ-42): intentional allowed case — ticket reference present.
export function tracked(): number {
  return 1;
}
